import React from "react";
import {
  HeroContainer,
  HeroP,
} from "./HeroElements";

const HeroSection = () => {
  return (
    <HeroContainer id="home">
      <HeroP>Department of Space, Indian Space Research Organisation</HeroP>
    </HeroContainer>
  );
};

export default HeroSection;
